#include<iostream>
using namespace std;

int main()
{
    int a=54;
    /*int& b =a; // In this int & is use for adrress ,in this b=a means it set the address of b to the address of a
    cout<<b;*/

    //int* ptr=&a  // int this the address of a is store to the ptr variables
}